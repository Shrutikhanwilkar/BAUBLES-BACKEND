doctype html
html
  body(style="font-family:Arial; background:#f6f9fb; padding:20px;")
    table(style="max-width:600px; margin:auto; background:#fff; padding:24px; border-radius:8px;")
      tr
        td
          h3 Hello #{name},

          p Your account has been created successfully.

          p(style="margin:16px 0;")
            | Your temporary password is:

          p(style="font-size:22px; font-weight:bold; background:#f1f7ff; padding:12px; border-radius:6px; display:inline-block;")
            | #{password}

          p(style="margin-top:16px; color:#555;")
            | Please log in and change your password immediately.

          p(style="margin-top:20px; font-size:14px; color:#777;")
            | If you did not request this, please contact support immediately.

          p(style="margin-top:24px;")
            | Regards,
            br
            | Team Baubles
