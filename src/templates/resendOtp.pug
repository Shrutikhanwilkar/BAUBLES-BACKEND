//- templates/email-verification-otp.pug
doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body(style="font-family: Arial, sans-serif; color: #333; line-height:1.5; background:#f6f9fb; padding:20px;")
    table(width="100%" cellpadding="0" cellspacing="0" style="max-width:600px; margin:0 auto; background:#fff; border-radius:8px; padding:24px;")
      tr
        td
          h3(style="margin:0 0 12px;") Hello #{name},

          p(style="margin:0 0 16px;")
              br
              | As requested, here is your new OTP :

          p(style="font-size:26px; font-weight:bold; letter-spacing:3px; margin:10px 0;")
            span(style="background:#f1f7ff; padding:12px 24px; border-radius:6px; display:inline-block;") #{otp}

          p(style="margin:14px 0; color:#555;")
            | This OTP will expire in 10 minutes.

          p(style="margin-top:18px; color:#666; font-size:14px;")
            | If you didn’t request this, you can safely ignore this email.

          hr(style="margin:24px 0; border:none; border-top:1px solid #eee;")

          p(style="margin:0;") Warm regards,
          p(style="margin:0; font-weight:bold;") Team Baubles
